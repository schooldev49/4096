generateTilePosition = (table, type) => { 
  switch(type){
    case "random:
      // go through and find a random
      // return coordinates
    case "rigged":
      // go through and find worst possible combo
      // return coordinates
    default:
      return "[internal error]: no type given"
  }
}

export default generateTilePosition;